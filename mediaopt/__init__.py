"""MediaOpt — a fed-batch media design and mass-balance engine.

The calculation engine (``mediaopt.calculations``) is pure Python and knows nothing
about the web layer. Every function takes plain arguments and returns a plain dict,
so the science can be unit-tested on its own.
"""
__version__ = "0.1.0"
